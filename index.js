import { registerRootComponent } from 'expo';
import App from './App'; // App.js é o componente principal

// Registrar o componente principal do aplicativo para que o Expo o use
registerRootComponent(App);
